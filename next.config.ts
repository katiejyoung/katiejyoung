import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // basePath: "/katiejyoung",
  // output: "export",  // enables github hosting
  // reactStrictMode: true,
};

export default nextConfig;
